# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# The following keys must be escaped otherwise they will not be retrieved by
# the default I18n backend:
#
# true, false, on, off, yes, no
#
# Instead, surround them with single quotes.
#
# en:
#   'true': 'foo'
#
# To learn more, please read the Rails Internationalization guide
# available at https://guides.rubyonrails.org/i18n.html.

en:
  hello: "Hello world"
  school:
    sync_status:
      never: Never synced
      queued: Queued
      syncing: Syncing
      successful: Synced
      failed: Failed
      needed: Sync needed
      stalled: Sync timed out
      unknown: Unknown
  activerecord:
    attributes:
      school:
        client_id: Client ID
    errors:
      models:
        school:
          wonde_unavailable: "Wonde could not supply this school. Check the Client ID and token, or try again later."
          attributes:
            client_id:
              not_on_wonde: "is not a school this token can read on Wonde"
            token:
              refused_by_wonde: "was refused by Wonde"
        school_group:
          restrict_dependent_destroy:
            has_many: "This group still has schools. Move them to another group, or leave them ungrouped, before deleting it."
